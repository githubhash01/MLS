## Implementation
- Make sure KNN/ANN can handle large datasets -- implement batching mechanism to avoid OOM issues
    - First, check piazza: possible that worrying about this is unnecessary
- Profile new KNN, K means, and ANN functions to make sure there is no glaring performance issue
    - If there is something, fix it
- Check over batch processing mechanism
- Try doing ANN with text-based dataset
    - Hugging face BioAsq dataset


## Testing and Benchmarking
Task 1
- GPU speed advantage for different dist. functions, KNN, and KNN using different distance functions
- When processing 4,000 vectors, how many seconds does the operation take? Furthermore, when handling 4,000,000 vectors, what modifications did you implement to ensure the effective functioning of your code?

Task 2
- Test recall from varying N, D, K, num_clusters
- GPU speed advantage for D=2 vs D=1024
- Compare numpy vs cupy vs torch?